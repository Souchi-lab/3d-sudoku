# 🎲 3D対戦数独アプリ - Kivy版

**3×3×3の立体数独盤を、2人で交互に配置しながら戦う新感覚パズルゲーム！**  
Python + Kivyで動作する立体GUIアプリです。タッチ／ズーム／スライス操作も対応！  

---

## 🚀 特徴

- 🎮 **立体3Dキューブ表示**（27セル）＋ タッチでの視点回転／ズーム操作
- 👯 **2人対戦モード**（交互ターン制・得点加算）
- 🧠 **ルール検証／候補数値管理**（`Field.py`）
- 🔢 **数字順提示システム**（ランダム27個）
- ✅ **ルール違反は即ポップアップで通知**
- 💾 **ログ出力機能あり** → `./log/4cube.log`
- ♻️ **リセットボタン**でスコア＆盤面初期化

---

## 🧊 新機能：スライスビュー（Slice View）

画面右側には「X/Z 軸ごとのスライスビュー」を搭載！  
対象軸＆層番号を選ぶことで、27セルから絞った可視範囲で配置操作が可能です。

### スライスビューの特徴：

| 項目 | 全体表示 (left) | スライスビュー (right) |
|------|------------------|--------------------------|
| 視点回転 | ✅ タッチ対応        | ❌ 固定視点 |
| ズーム     | ✅ ＋ZOOM/−ZOOM    | ❌ 固定ズーム |
| 数字選択 | ❌ 無効             | ✅ 有効（スライス内） |
| スライス選択 | ❌                 | ✅ 軸＆深度で絞り込み可 |

---

## 🖼 UI全体構成（イメージ）

### 📷 実装画面サンプル

![UI構成図（実装画面）](73de7c5c-4abb-485e-a8f7-b48ce7840aaf.png)

---

### 🗺 Mermaid による構造図（クリック可能）

```mermaid
flowchart LR
  subgraph ヘッダー
    H1[P1: 0pt   P2: 0pt]
    H2[現在: 1   次: 3,3,1,2,1…]
  end

  subgraph 本体
    direction LR
    A[3D全体表示キャンバス]
    B[スライスビューキャンバス]
  end

  subgraph コントロール
    direction LR
    C[SPIN X+][SPIN X-][SPIN Y+][SPIN Y-][+ZOOM][-ZOOM]
    D[Axis: (X)(Z) Depth: ――――◉――――]
    E[RESET]
  end

  H1 & H2 --> A
  H1 & H2 --> B
  A --> C
  B --> D
  C & D --> E
```

---

## 🌟 総合評価

このプロジェクトは「3D対戦数独」というユニークなコンセプトを、Kivyを用いて見事に実現しています。特に以下の点で優れています。

- **明確な関心の分離**: UI (`cube3d_view.py`)、ゲーム状態 (`game_state.py`)、コアロジック (`cube_logic.py`, `Field.py`) が適切に分離されており、コードの可読性とメンテナンス性が高い設計です。
- **豊富な機能**: 3D/2Dビューの連携、対戦モード、CPU機能、リセット機能など、ゲームとして必要な機能が一通り実装されています。
- **設定の外部化**: `Constants.py` にゲームの主要な設定がまとめられており、バランス調整が容易です。
- **丁寧なドキュメント**: READMEには機能概要やUI構成図が含まれており、プロジェクトの全体像を素早く理解できます。

全体として、完成度の高いアプリケーションの素晴らしい基盤ができています。

---

## 🌟 ルール
        [b]3D 対戦数独アプリ[/b]

        [b]目的:[/b]
        3x3x3の立体数独盤に、交互に数字を配置していきます。
        最終的に、より多くのセルに正しく数字を配置できたプレイヤーの勝利です。

        [b]ルール:[/b]
        1. 各セルには1から3までの数字を配置できます。
        2. 同じ数字は、以下のいずれかのライン上に重複して配置できません。
           - X軸方向のライン (横一列)
           - Y軸方向のライン (縦一列)
           - Z軸方向のライン (奥行き一列)
        3. 配置する数字は、画面上部に表示される「Number: [数字]」に従います。
        4. 配置可能なセルは、スライスビューで選択し、再度タップすることで確定します。
        5. ルール違反の配置はできません。

        [b]操作方法:[/b]
        - [b]3D全体表示 (左側):[/b]
          - ドラッグ: 視点回転
          - スクロール/ZOOMボタン: ズームイン/アウト
          - RESET VIEWボタン: 視点を初期状態に戻す
        - [b]スライスビュー (右側):[/b]
          - Axis (X/Z): 表示する軸を選択
          - Depthスライダー: 表示する層を選択
          - セルタップ: セルを選択 (ハイライト表示)
          - 選択中のセルを再度タップ: 数字を配置 (ルールに従って)

        [b]スコア:[/b]
        - 数字を配置するごとにスコアが加算されます。
        - 最終的なスコアで勝敗が決まります。

---

## 💡 現在の状況

- Kivy Language (`.kv` ファイル) を使用したUIの分離を試みましたが、レイアウトの問題が発生したため、一時的に元のPythonコードベースに戻しました。
- `moderngl` を使用した3D描画への移行も試みましたが、これも一時中断し、元のKivyグラフィックス描画に戻しました。
- 現在のアプリケーションは、以前のKivyグラフィックスとPythonコードベースのUIで動作しています。
- 3Dキューブのサイズ調整は完了し、適切に表示されています。
- ロジックの確認は可能な状態です。

---

## ✅ 改善ポイント（TODO）

今後さらに洗練されたアプリを目指して、既存のアイデアに加えて技術的な観点からの改善案を以下にまとめました。

### 🧹 アーキテクチャ・コード整理 (Architecture & Refactoring)

- [x] **不要なファイルの削除**: `cube3d.py`, `MyLogic.py`, `Player.py`, `rotation_ui.py` は、現在使用されていないか、他のファイルと機能が重複している可能性があります。コードベースを整理するために、これらのファイルを削除または統合することを推奨します。
- [x] **マジックナンバーの削減**: `range(3)` のように直接記述されている数値を `Constants.py` の `LENGTH_OF_SIDE` を使うように統一し、将来的な拡張性を高めます。
- [x] **CPUプレイヤーのハードコーディング解消**: `main.py` 内でCPUのプレイヤーIDが `2` に固定されています。これを設定ファイルや`GameState`で管理するように変更し、柔軟性を向上させます。

### 🧠 機能改善・拡張 (Feature Enhancements)

- [x] **CPUロジックの強化**: 現在のCPUはランダムな空きマスに配置するだけです。次のステップとして、少なくともルール上配置可能なマスを探して配置するような、より「賢い」ロジックへの改善が考えられます。
- [x] **ゲーム終了条件の明確化**: 全てのセルが埋まった時点でのスコアで勝敗を判定するなど、明確なゲームの終了条件と勝利判定を実装します。
- [x] **初期配置モード**: `README`のTODOにもある通り、いくつかの数字が事前に配置された「詰め数独」モードを追加すると、ゲームのバリエーションが広がります。

### 🎨 UI/UX改善 (UI/UX Improvements)

- [x] **現在のプレイヤーを明示**: スコア表示に加え、現在どちらのプレイヤーのターンで、どの数字を配置する番なのかを画面上部でハイライト表示します。
- [x] **入力セルの事前選択**: （既存TODO）スライスビューでセルをタップした際に、まず「選択状態」とし、確認操作（例：もう一度タップ、または確定ボタン）を経て数字を配置する2段階入力にすると、誤操作を防げます。
- [x] **視点リセット機能**: （既存TODO）`SPIN RESET`ボタンを追加し、カメラの角度やズームを初期状態に戻せるようにします。
- [x] **ルール説明の追加**: （既存TODO）アプリ内に簡単なルール説明（勝利条件、操作方法）を表示する画面を追加します。
- **モバイル対応のレイアウト調整の再検討**: Kivyのレイアウトシステムをより深く理解し、`size_hint` や `pos_hint` を活用して、様々な画面サイズに対応できる柔軟なUIレイアウトを構築する。
- **3D描画の再検討**: `moderngl` を使用した3D描画の導入は、より高度なビジュアルとパフォーマンス向上に繋がるが、段階的に進めるか、あるいはGodot Engineのようなより統合された3Dフレームワークへの移行を検討する。
- **全体的なデザインの洗練**: 色使い、フォント、アイコンなどを統一し、よりモダンでスタイリッシュなUIデザインを目指す。

### 🛠️ 開発・保守性 (Development & Maintainability)

- [x] **ユニットテストの導入**: `Field.py` のルール判定や `game_state.py` の状態遷移など、コアロジックに対するユニットテストを作成し、コードの品質と安定性を向上させます。
- [x] **依存関係の整理**: `requirements.txt` に記載されている `numpy` や `pyopengl` が直接必要かを確認し、不要であれば削除またはコメントを追加します。

---

## 🌟 さらなる改善点

- [x] **リファクタリング**: `main.py` の `on_success_input` メソッドが複数の責務を持ちすぎているため、ゲーム進行、UI更新、CPU操作などのロジックを分離し、可読性を向上させます。
- [x] **UI/UXの向上**: ゲーム終了時のポップアップに「リセット」「終了」ボタンを追加し、操作性を向上させます。また、現在のプレイヤーのターンを視覚的に強調する（例：ラベルの色を変更する）などの改善も考えられます。
- [x] **テストの拡充**: UI操作（ボタンクリックなど）や `Field.py` のロジックに対するテストを追加し、品質をさらに高めます。

---

ご要望・改善提案はいつでも大歓迎です！  
Pull RequestやIssueでの貢献、お待ちしています！✨